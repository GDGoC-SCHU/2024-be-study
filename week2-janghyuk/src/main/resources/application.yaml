spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gdg_study # ?????? URL
    username: root  # ?????? ????
    password:  # ?????? ????
    driver-class-name: com.mysql.cj.jdbc.Driver  # MySQL ???? ???
  jpa:
    hibernate:
      ddl-auto: validate  # ??? ?? ?? ?? (create, update, validate, none ? ??)
      show-sql: true  # SQL ??? ??? ??
    properties:
      hibernate:
      format_sql: true  # SQL? ???? ??